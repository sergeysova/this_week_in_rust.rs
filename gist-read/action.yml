name: 'ThisWeekInRust Gist Read Action'
description: 'Action to get gist data'
inputs:
  gist_id:
    description: 'Gist ID'
    required: true
  file_name:
    description: 'Telegram chat ID or @username'
    required: true
  # github_token:
  #   description: 'Github Access Token'
outputs:
  last_id:
    description: 'last read id'
runs:
  using: 'docker'
  image: 'Dockerfile'
  # env:
  #   GITHUB_TOKEN: ${{ inputs.github_token }}
